import React from 'react'
import { Route, Routes,Outlet } from "react-router-dom";

const About = () => {
  return (
    <> 
    <div>about page </div>
    <Outlet/>
    </>
    
  )
}

export default About